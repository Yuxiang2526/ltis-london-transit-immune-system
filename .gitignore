# Local editor and assistant state
.claude/
.vscode/
.idea/
.tmp-chrome-dump/
docs/screenshots/

# Local/legacy source copies of Siyan's PTAL map — the active copy lives in
# web/public/network-map/ and IS committed (so GitHub Pages serves it as-is).
London_PTAL_Accessibility_Map(1)/
London_PTAL_Accessibility_Map/

# Build output (Vite generates this from web/src/ + web/public/)
web/dist/

# Node modules
node_modules/
web/node_modules/
analysis/node_modules/

# Zips and intermediate dumps that we don't want in the repo
*.zip
!web/public/network-map/route_lines.geojson  # safety: never accidentally ignore the route geojson

# OS files
.DS_Store
Thumbs.db

# Logs and local environment
*.log
.env
.env.*
!.env.example

# Python/Jupyter scratch files
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.ipynb_checkpoints/
*.nbconvert.ipynb
