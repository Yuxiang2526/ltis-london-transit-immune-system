# Local editor and assistant state
.claude/
.vscode/
.idea/

# Local/legacy copy of Siyan's PTAL map source — not used by the LTIS Vite deploy.
# The active copy lives in web/public/network-map/ (with large data git-ignored below).
London_PTAL_Accessibility_Map(1)/
London_PTAL_Accessibility_Map/

# Large network-map data: shipped via GitHub Release v1.0-data, NOT committed.
# Local dev keeps these in web/public/network-map/ for npm run dev.
# At deploy time, an action downloads them from the release tag into the same path.
web/public/network-map/data_chunks_osm_network/
web/public/network-map/grid_ai_chunks_osm_network/
web/public/network-map/DATA/PTAL_2023_Grid_100mx100m_Data.geojson

# OS files
.DS_Store
Thumbs.db

# Logs and local environment
*.log
.env
.env.*
!.env.example

# Python/Jupyter scratch files
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.ipynb_checkpoints/
*.nbconvert.ipynb
